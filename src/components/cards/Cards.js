import React from "react";


export const Cards = ({ data }) => {
  <article className="card-box">
    {/* {data && data.length ? data.map(card =>
      <article className="card-content">
        <img className="card-img" src={"/" + card.img.product} />

      </article>
    )} */}
  </article>
}